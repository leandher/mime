import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    backgroundColor: '#333333',
    height: 54,
    flexDirection: 'row',
    borderRadius: 25,
    overflow: 'hidden',
    alignItems: 'center',
    marginTop: 16,
    width: 263,
  },
});
